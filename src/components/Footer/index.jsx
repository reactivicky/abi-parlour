import { Container, UpButton } from "./style";

const Footer = () => {
  return (
    <Container className="clr_white">
      <UpButton variant="secondary">Up</UpButton>
    </Container>
  );
};

export default Footer;
